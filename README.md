# Angel Name Generator (Angular)

This is a minimal Angular 21.2.6 app that uses `AngelNameGeneratorService` to produce angel names.

Quick start:

1. npm install
2. npm start

The app will open in your browser (ng serve).
